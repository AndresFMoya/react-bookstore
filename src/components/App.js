import React from 'react';
import BooksList from '../containers/BooksList';
import BooksForm from './BooksForm';

const App = () => {
  return(
  <div className="app">
    <BooksList />
  </div>
  );
};

export default App;
